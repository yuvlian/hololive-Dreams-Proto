syntax = "proto3";

import "common/reward.gen.proto";
import "entity/master/options/master.proto";

package entity.master;

option go_package = "github.com/qua-vision/vision-server/pkg/pb/master";
option csharp_namespace = "Vision.Common.Proto.Master";

message MissionProgress {
	option (options.master.table) = true;

	string mission_id = 1 [(options.master.pk) = true, (options.master.fk) = "Mission:id"];
	int64 threshold = 2 [(options.master.pk) = true];
	int64 reward_mission_pass_point_quantity = 4 [(options.master.optional) = true];
	int64 reward_character_exp_quantity = 5 [(options.master.optional) = true];
	repeated .common.Reward rewards = 1000 [(options.master.optional) = true];
}
