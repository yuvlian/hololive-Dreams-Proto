syntax = "proto3";

package enums;

option go_package = "github.com/qua-vision/vision-server/pkg/domain/enum";
option csharp_namespace = "Vision.Common.Proto.Enums";

enum DeviceWorkaroundType {
	DEVICE_WORKAROUND_TYPE_UNKNOWN = 0;
	DEVICE_WORKAROUND_TYPE_TEXTURE_COMPOSITE_INPUT_ATTACHMENT = 1;
	DEVICE_WORKAROUND_TYPE_LIVE2D_PASS_MERGE = 2;
	DEVICE_WORKAROUND_TYPE_PIXEL10_WARM_UP = 3;
	DEVICE_WORKAROUND_TYPE_TEXTURE_STREAMING_LIMIT = 4;
	DEVICE_WORKAROUND_TYPE_COLOR_LOAD_OP_CLEAR_DRAW = 5;
}
